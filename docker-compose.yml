buildbotmaster:
    extends:
        file: ./common/docker-compose.yml
        service: commonmaster
    ports:
        - "8011:8010"

buildbotslave:
    extends:
        file: ./common/docker-compose.yml
        service: commonslave
    links:
        - buildbotmaster

